算法:
    1.冒泡排序
    2.选择排序
    3.插入排序


程序:
    bin
    config
    db
    lib
    src


反射:利用字符串的形式去对象中(模块)中操作(查找/检测/添加/删除)成员
    getattr,
    delattr,
    setattr,
    hasattr


模块:
    vars()           # 全部变量
    __builtins__     # 内置所有函数
    __file__         # 当前py文件的路径,有坑,在当前目录下只能得到文件名
    __package__      # 当前在哪个包里
    __cached__       # pyc文件路径
    __doc__          # 这个文件的注视,顶行"""注释内容
    __name__         # 只有执行当前文件时候,当前文件的特殊变量__name__ == mian



re(正则):
    [ ] 中除"-"其它特殊字符无意义
            ^  非

    \
        \d
        \s
        \w
        \b

    正则分组
        去已经匹配到的数据中再提取数据